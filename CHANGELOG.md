# Changelog

## Unreleased

- `to_entities()` and `to_documents()` now accept a `field_names` parameter for remapping source field names to spaneval's canonical names (e.g. spaCy, HuggingFace output).

## 0.1.0 (2026-03-07)

- Initial release.
